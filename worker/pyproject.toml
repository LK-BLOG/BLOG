[project]
name = "xiaokan-api"
version = "0.1.0"
description = "小戡的博客 API（FastAPI on Cloudflare Workers）"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
]

[dependency-groups]
dev = [
    "workers-py",
    "workers-runtime-sdk",
]